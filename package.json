{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "opencode-workspace",
  "private": true,
  "type": "module",
  "packageManager": "bun@1.3.14",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "bun run --filter '*' build",
    "test": "bun run --filter '*' test",
    "typecheck": "bun run --filter '*' typecheck",
    "verify": "bun run typecheck && bun run test && bun run build",
    "publish:alpha": "bun run verify && npm publish --workspace codex-delegator --tag alpha --access public && npm publish --workspace opencode-codex-delegate --tag alpha --access public && npm publish --workspace opencode-goal --tag alpha --access public && npm publish --workspace opencode-multimodel --tag alpha --access public && npm publish --workspace @proofofwork-agency/opencode-btw --tag alpha --access public"
  }
}
